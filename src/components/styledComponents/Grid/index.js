import Grid from '@mui/material/Grid';
import { styled } from '@mui/material/styles';

const GridPrimary = styled(Grid)(({ margin, padding }) => ({
  margin,
  padding
}));

export {
  GridPrimary
};
